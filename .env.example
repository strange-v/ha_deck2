# Generate with: openssl rand -base64 32
ESPHOME_API_ENCRYPTION_KEY=replace-with-your-base64-key

# Optional: private replacement for the bundled Meteocons weather SVGs.
# HA_DECK_WEATHER_ICONS_DIR=/path/to/private/weather-icons
